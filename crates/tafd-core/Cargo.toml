[package]
name = "tafd-core"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbeam = "0.8"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dependencies.directories]
version = "5.0"
optional = true

[features]
default = ["directories"]
